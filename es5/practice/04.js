{
  /**
   * const d = new Date();
   * d.getTime(); = Date.now() 返回时间戳
   * Date.parse(d); // Date.parse('2022-02-13') 返回时间戳
   * 
   * const d = new Date();
   * const y = d.getFullYear();
   * const m = d.getMonth() + 1; // 0 - 11
   * const day = d.getDate();
   * const weekday = d.getDay() + 1;
   * const h = d.getHours();
   * const mi = d.getMinutes()
   * const s = d.getSeconds();
   * console.log(`${y}年${m}月${day}日 星期${weekday} ${h}:${mi}:${s}`);
   */
  /**
   * const d = new Date();
   * d.setFullYear(2021);
   * d.setMonth(11);
   * d.setDate(31);
   * d.setHours(8);
   * d.setMinutes(30);
   * d.setSeconds(30);
   */
}
